---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: contaboclusters.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: ContaboCluster
    listKind: ContaboClusterList
    plural: contaboclusters
    singular: contabocluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Cluster to which this ContaboCluster belongs
      jsonPath: .metadata.labels.cluster\.x-k8s\.io/cluster-name
      name: Cluster
      type: string
    - description: Cluster infrastructure is ready
      jsonPath: .status.ready
      name: Ready
      type: string
    - description: Private Networks
      jsonPath: .status.privateNetworks[].name
      name: Private Networks
      type: string
    - description: API Endpoint
      jsonPath: .spec.controlPlaneEndpoint.host
      name: Endpoint
      priority: 1
      type: string
    name: v1beta2
    schema:
      openAPIV3Schema:
        description: ContaboCluster is the Schema for the contaboclusters API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ContaboCluster
            properties:
              controlPlaneEndpoint:
                description: ControlPlaneEndpoint represents the endpoint used to
                  communicate with the control plane.
                minProperties: 1
                properties:
                  host:
                    description: host is the hostname on which the API server is serving.
                    maxLength: 512
                    minLength: 1
                    type: string
                  port:
                    description: port is the port on which the API server is serving.
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                type: object
              privateNetworks:
                description: PrivateNetwork specifies the private network configuration
                  for the cluster.
                properties:
                  region:
                    description: Region Region where the Private Network should be
                      located. Default is `EU`
                    type: string
                required:
                - region
                type: object
            required:
            - privateNetworks
            type: object
          status:
            description: status defines the observed state of ContaboCluster
            properties:
              clusterUUID:
                description: ClusterUUID is the identifier of the Contabo cluster.
                type: string
              conditions:
                description: Conditions defines current service state of the ContaboCluster.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              failureDomains:
                description: FailureDomains is a list of failure domains that machines
                  can be placed in.
                items:
                  description: |-
                    FailureDomain is the Schema for Cluster API failure domains.
                    It allows controllers to understand how many failure domains a cluster can optionally span across.
                  properties:
                    attributes:
                      additionalProperties:
                        type: string
                      description: attributes is a free form map of attributes an
                        infrastructure provider might use or require.
                      type: object
                    controlPlane:
                      description: controlPlane determines if this failure domain
                        is suitable for use by control plane machines.
                      type: boolean
                    name:
                      description: name is the name of the failure domain.
                      maxLength: 256
                      minLength: 1
                      type: string
                  required:
                  - name
                  type: object
                type: array
              initialization:
                description: Initialization
                properties:
                  provisioned:
                    description: Provisioned indicates if the initialization is complete
                    type: boolean
                required:
                - provisioned
                type: object
              privateNetworks:
                description: PrivateNetwork contains the discovered information about
                  private networks
                properties:
                  availableIps:
                    description: AvailableIps The total available IPs of the Private
                      Network
                    format: int64
                    type: integer
                  cidr:
                    description: Cidr The cidr range of the Private Network
                    type: string
                  createdDate:
                    description: CreatedDate The creation date of the Private Network
                    format: int64
                    type: integer
                  customerId:
                    description: CustomerId Your customer number
                    type: string
                  dataCenter:
                    description: DataCenter The data center where your Private Network
                      is located
                    type: string
                  description:
                    description: Description The description of the Private Network
                    type: string
                  instances:
                    items:
                      description: Instances defines model for Instances.
                      properties:
                        displayName:
                          description: DisplayName Instance display name
                          type: string
                        errorMessage:
                          description: ErrorMessage Message in case of an error.
                          type: string
                        instanceId:
                          description: InstanceId Instance id
                          format: int64
                          type: integer
                        ipConfig:
                          description: IpConfig defines model for IpConfig.
                          properties:
                            v4:
                              description: IpV4 defines model for IpV4.
                              properties:
                                gateway:
                                  description: Gateway Gateway
                                  type: string
                                ip:
                                  description: Ip IP Address
                                  type: string
                                netmaskCidr:
                                  description: NetmaskCidr Netmask CIDR
                                  format: int32
                                  type: integer
                              required:
                              - gateway
                              - ip
                              - netmaskCidr
                              type: object
                            v6:
                              description: IpV6 defines model for IpV6.
                              properties:
                                gateway:
                                  description: Gateway Gateway
                                  type: string
                                ip:
                                  description: Ip IP Address
                                  type: string
                                netmaskCidr:
                                  description: NetmaskCidr Netmask CIDR
                                  format: int32
                                  type: integer
                              required:
                              - gateway
                              - ip
                              - netmaskCidr
                              type: object
                          required:
                          - v4
                          - v6
                          type: object
                        name:
                          description: Name Instance name
                          type: string
                        privateIpConfig:
                          description: PrivateIpConfig defines model for PrivateIpConfig.
                          properties:
                            v4:
                              items:
                                description: IpV4 defines model for IpV4.
                                properties:
                                  gateway:
                                    description: Gateway Gateway
                                    type: string
                                  ip:
                                    description: Ip IP Address
                                    type: string
                                  netmaskCidr:
                                    description: NetmaskCidr Netmask CIDR
                                    format: int32
                                    type: integer
                                required:
                                - gateway
                                - ip
                                - netmaskCidr
                                type: object
                              type: array
                          required:
                          - v4
                          type: object
                        productId:
                          description: ProductId Product id
                          type: string
                        status:
                          description: Status State of the instance in the Private
                            Network
                          type: string
                      required:
                      - displayName
                      - instanceId
                      - ipConfig
                      - name
                      - privateIpConfig
                      - productId
                      - status
                      type: object
                    type: array
                  name:
                    description: Name The name of the Private Network
                    type: string
                  privateNetworkId:
                    description: PrivateNetworkId Private Network's id
                    format: int64
                    type: integer
                  region:
                    description: Region The slug of the region where your Private
                      Network is located
                    type: string
                  regionName:
                    description: RegionName The region where your Private Network
                      is located
                    type: string
                  tenantId:
                    description: TenantId Your customer tenant id
                    type: string
                required:
                - availableIps
                - cidr
                - createdDate
                - customerId
                - dataCenter
                - description
                - instances
                - name
                - privateNetworkId
                - region
                - regionName
                - tenantId
                type: object
              ready:
                description: Ready denotes that the cluster (infrastructure) is ready.
                type: boolean
              secrets:
                description: SshKey contains the references to secrets used by the
                  machine.
                properties:
                  name:
                    description: Name is the name of the SSH key
                    type: string
                  secretId:
                    description: SecretId is the ID of the SSH key in Contabo
                    format: int64
                    type: integer
                  value:
                    description: Value is the actual SSH public key value
                    type: string
                required:
                - name
                - secretId
                - value
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
