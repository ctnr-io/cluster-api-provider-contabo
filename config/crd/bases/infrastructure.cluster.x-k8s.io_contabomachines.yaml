---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: contabomachines.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: ContaboMachine
    listKind: ContaboMachineList
    plural: contabomachines
    singular: contabomachine
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Cluster to which this ContaboMachine belongs
      jsonPath: .metadata.labels.cluster\.x-k8s\.io/cluster-name
      name: Cluster
      type: string
    - description: Contabo instance type
      jsonPath: .spec.instanceType
      name: InstanceType
      type: string
    - description: Contabo instance state
      jsonPath: .status.instanceState
      name: State
      type: string
    - description: Machine ready status
      jsonPath: .status.ready
      name: Ready
      type: string
    - description: Contabo instance ID
      jsonPath: .spec.providerID
      name: ProviderID
      type: string
    - description: Machine object which owns this ContaboMachine
      jsonPath: .metadata.ownerReferences[?(@.kind=="Machine")].name
      name: Machine
      type: string
    name: v1beta2
    schema:
      openAPIV3Schema:
        description: ContaboMachine is the Schema for the contabomachines API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ContaboMachine
            properties:
              instance:
                description: Instance is the type of instance to create.
                properties:
                  addOns:
                    description: AddOns Set attributes in the addons object for the
                      corresponding ones that need to be added to the instance
                    properties:
                      additionalIps:
                        additionalProperties:
                          type: string
                        description: AdditionalIps Set this attribute if you want
                          to upgrade your instance with the Additional IPs addon.
                          Please provide an empty object for the time being as value.
                          There will be more configuration possible in the future.
                        type: object
                      addonsIds:
                        items:
                          description: AddOnRequest defines model for AddOnRequest.
                          properties:
                            id:
                              description: Id Id of the Addon. Please refer to list
                                [here](https://contabo.com/en/product-list/?show_ids=true).
                              format: int64
                              type: integer
                            quantity:
                              description: Quantity The number of Addons you wish
                                to aquire.
                              format: int64
                              type: integer
                          required:
                          - id
                          - quantity
                          type: object
                        type: array
                      backup:
                        additionalProperties:
                          type: string
                        description: Backup Set this attribute if you want to upgrade
                          your instance with the Automated backup addon.     Please
                          provide an empty object for the time being as value. There
                          will be more configuration possible     in the future.
                        type: object
                      customImage:
                        additionalProperties:
                          type: string
                        description: CustomImage Set this attribute if you want to
                          upgrade your instance with the Custom Images addon.   Please
                          provide an empty object for the time being as value. There
                          will be more configuration possible   in the future.
                        type: object
                      extraStorage:
                        description: ExtraStorage Set this attribute if you want to
                          upgrade your instance with the Extra Storage addon.
                        properties:
                          nvme:
                            description: Nvme Specify the size in TB and the quantity
                            items:
                              type: string
                            type: array
                          ssd:
                            description: Ssd Specify the size in TB and the quantity
                            items:
                              type: string
                            type: array
                        type: object
                      privateNetworking:
                        additionalProperties:
                          type: string
                        description: PrivateNetworking Set this attribute if you want
                          to upgrade your instance with the Private Networking addon.   Please
                          provide an empty object for the time being as value. There
                          will be more configuration possible   in the future.
                        type: object
                    type: object
                  applicationId:
                    description: ApplicationId Application ID
                    type: string
                  defaultUser:
                    description: DefaultUser Default user name created for login during
                      (re-)installation with administrative privileges. Allowed values
                      for Linux/BSD are `admin` (use sudo to apply administrative
                      privileges like root) or `root`. Allowed values for Windows
                      are `admin` (has administrative privileges like administrator)
                      or `administrator`.
                    type: string
                  displayName:
                    description: DisplayName The display name of the instance
                    type: string
                  imageId:
                    description: ImageId ImageId to be used to setup the compute instance.
                      Default is Ubuntu 22.04
                    type: string
                  license:
                    description: License Additional licence in order to enhance your
                      chosen product, mainly needed for software licenses on your
                      product (not needed for windows).
                    type: string
                  period:
                    description: 'Period Initial contract period in months. Available
                      periods are: 1, 3, 6 and 12 months. Default to 1 month'
                    format: int64
                    type: integer
                  productId:
                    description: ProductId Default is V92
                    type: string
                  region:
                    description: Region Instance Region where the compute instance
                      should be located. Default is EU
                    type: string
                  rootPassword:
                    description: RootPassword `secretId` of the password for the `defaultUser`
                      with administrator/root privileges. For Linux/BSD please use
                      SSH, for Windows RDP. Please refer to Secrets Management API.
                    format: int64
                    type: integer
                  sshKeys:
                    description: SshKeys Array of `secretId`s of public SSH keys for
                      logging into as `defaultUser` with administrator/root privileges.
                      Applies to Linux/BSD systems. Please refer to Secrets Management
                      API.
                    items:
                      format: int64
                      type: integer
                    type: array
                  userData:
                    description: UserData [Cloud-Init](https://cloud-init.io/) Config
                      in order to customize during start of compute instance.
                    type: string
                required:
                - period
                type: object
              privateNetworks:
                description: PrivateNetworks specifies the network configuration for
                  the machine.
                items:
                  description: CreatePrivateNetworkRequest defines model for CreatePrivateNetworkRequest.
                  properties:
                    description:
                      description: Description The description of the Private Network.
                        There is a limit of 255 characters per Private Network description.
                      type: string
                    name:
                      description: Name The name of the Private Network. It may contain
                        letters, numbers, colons, dashes, and underscores. There is
                        a limit of 255 characters per Private Network name.
                      type: string
                    region:
                      description: Region Region where the Private Network should
                        be located. Default is `EU`
                      type: string
                  required:
                  - name
                  type: object
                type: array
              providerID:
                description: ProviderID is the unique identifier as specified by the
                  cloud provider.
                type: string
              secrets:
                description: Secrets references the secret with user data to configure
                  the machine.
                items:
                  description: CreateSecretRequest defines model for CreateSecretRequest.
                  properties:
                    name:
                      description: Name The name of the secret that will keep the
                        password
                      type: string
                    type:
                      description: Type The type of the secret. Can be `password`
                        or `ssh`
                      type: string
                    value:
                      description: 'Value The secret value that needs to be saved.
                        In case of a password it must match a pattern with at least
                        one upper and lower case character and either one number with
                        two special characters `!@#$^&*?_~` or at least three numbers
                        with one special character `!@#$^&*?_~`. This is expressed
                        in the following regular expression: `^((?=.*?[A-Z]{1,})(?=.*?[a-z]{1,}))(((?=(?:[^d]*d){1})(?=([^^&*?_~]*[!@#$^&*?_~]){2,}))|((?=(?:[^d]*d){3})(?=.*?[!@#$^&*?_~]+))).{8,}$`'
                      type: string
                  required:
                  - name
                  - type
                  - value
                  type: object
                type: array
            required:
            - instance
            type: object
          status:
            description: status defines the observed state of ContaboMachine
            properties:
              addresses:
                description: Addresses contains the Contabo instance associated addresses.
                items:
                  description: MachineAddress contains information for the node's
                    address.
                  properties:
                    address:
                      description: address is the machine address.
                      maxLength: 256
                      minLength: 1
                      type: string
                    type:
                      description: type is the machine address type, one of Hostname,
                        ExternalIP, InternalIP, ExternalDNS or InternalDNS.
                      enum:
                      - Hostname
                      - ExternalIP
                      - InternalIP
                      - ExternalDNS
                      - InternalDNS
                      type: string
                  required:
                  - address
                  - type
                  type: object
                type: array
              conditions:
                description: Conditions defines current service state of the ContaboMachine.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              failureMessage:
                description: |-
                  FailureMessage will be set in the event that there is a terminal problem
                  reconciling the Machine and will contain a more verbose string suitable
                  for logging and human consumption.

                  This field should not be set for transitive errors that a controller
                  faces that are expected to be fixed automatically over
                  time (like service outages), but instead indicate that something is
                  fundamentally wrong with the Machine's spec or the configuration of
                  the controller, and that manual intervention is required. Examples
                  of terminal errors would be invalid combinations of settings in the
                  spec, values that are unsupported by the controller, or the
                  responsible controller itself being critically misconfigured.

                  Any transient errors that occur during the reconciliation of Machines
                  can be added as events to the Machine object and/or logged in the
                  controller's output.
                type: string
              failureReason:
                description: |-
                  FailureReason will be set in the event that there is a terminal problem
                  reconciling the Machine and will contain a succinct value suitable
                  for machine interpretation.

                  This field should not be set for transitive errors that a controller
                  faces that are expected to be fixed automatically over
                  time (like service outages), but instead indicate that something is
                  fundamentally wrong with the Machine's spec or the configuration of
                  the controller, and that manual intervention is required. Examples
                  of terminal errors would be invalid combinations of settings in the
                  spec, values that are unsupported by the controller, or the
                  responsible controller itself being critically misconfigured.

                  Any transient errors that occur during the reconciliation of Machines
                  can be added as events to the Machine object and/or logged in the
                  controller's output.
                type: string
              instance:
                description: Instance is the current state of the Contabo instance.
                properties:
                  addOns:
                    items:
                      description: AddOnResponse defines model for AddOnResponse.
                      properties:
                        id:
                          description: Id Id of the Addon. Please refer to list [here](https://contabo.com/en/product-list/?show_ids=true).
                          format: int64
                          type: integer
                        quantity:
                          description: Quantity The number of Addons you wish to aquire.
                          format: int64
                          type: integer
                      required:
                      - id
                      - quantity
                      type: object
                    type: array
                  additionalIps:
                    items:
                      description: AdditionalIp defines model for AdditionalIp.
                      properties:
                        v4:
                          description: IpV4 defines model for IpV4.
                          properties:
                            gateway:
                              description: Gateway Gateway
                              type: string
                            ip:
                              description: Ip IP Address
                              type: string
                            netmaskCidr:
                              description: NetmaskCidr Netmask CIDR
                              format: int32
                              type: integer
                          required:
                          - gateway
                          - ip
                          - netmaskCidr
                          type: object
                      required:
                      - v4
                      type: object
                    type: array
                  cancelDate:
                    description: CancelDate The date on which the instance will be
                      cancelled
                    type: string
                  cpuCores:
                    description: CpuCores CPU core count
                    format: int64
                    type: integer
                  createdDate:
                    description: CreatedDate The creation date for the instance
                    format: int64
                    type: integer
                  customerId:
                    description: CustomerId Customer ID
                    type: string
                  dataCenter:
                    description: DataCenter The data center where your Private Network
                      is located
                    type: string
                  defaultUser:
                    description: DefaultUser Default user name created for login during
                      (re-)installation with administrative privileges. Allowed values
                      for Linux/BSD are `admin` (use sudo to apply administrative
                      privileges like root) or `root`. Allowed values for Windows
                      are `admin` (has administrative privileges like administrator)
                      or `administrator`.
                    type: string
                  diskMb:
                    description: DiskMb Image Disk size in MB
                    type: number
                  displayName:
                    description: DisplayName Instance display name
                    type: string
                  errorMessage:
                    description: ErrorMessage Message in case of an error.
                    type: string
                  imageId:
                    description: ImageId Image's id
                    type: string
                  instanceId:
                    description: InstanceId Instance ID
                    format: int64
                    type: integer
                  ipConfig:
                    description: IpConfig defines model for IpConfig.
                    properties:
                      v4:
                        description: IpV4 defines model for IpV4.
                        properties:
                          gateway:
                            description: Gateway Gateway
                            type: string
                          ip:
                            description: Ip IP Address
                            type: string
                          netmaskCidr:
                            description: NetmaskCidr Netmask CIDR
                            format: int32
                            type: integer
                        required:
                        - gateway
                        - ip
                        - netmaskCidr
                        type: object
                      v6:
                        description: IpV6 defines model for IpV6.
                        properties:
                          gateway:
                            description: Gateway Gateway
                            type: string
                          ip:
                            description: Ip IP Address
                            type: string
                          netmaskCidr:
                            description: NetmaskCidr Netmask CIDR
                            format: int32
                            type: integer
                        required:
                        - gateway
                        - ip
                        - netmaskCidr
                        type: object
                    required:
                    - v4
                    - v6
                    type: object
                  macAddress:
                    description: MacAddress MAC Address
                    type: string
                  name:
                    description: Name Instance Name
                    type: string
                  osType:
                    description: OsType Type of operating system (OS)
                    type: string
                  productId:
                    description: ProductId Product ID
                    type: string
                  productName:
                    description: ProductName Instance's Product Name
                    type: string
                  productType:
                    description: ProductType Instance's category depending on Product
                      Id
                    type: string
                  ramMb:
                    description: RamMb Image RAM size in MB
                    type: number
                  region:
                    description: Region Instance region where the compute instance
                      should be located.
                    type: string
                  regionName:
                    description: RegionName The name of the region where the instance
                      is located.
                    type: string
                  sshKeys:
                    description: SshKeys Array of `secretId`s of public SSH keys for
                      logging into as `defaultUser` with administrator/root privileges.
                      Applies to Linux/BSD systems. Please refer to Secrets Management
                      API.
                    items:
                      format: int64
                      type: integer
                    type: array
                  status:
                    description: Status Instance's status
                    type: string
                  tenantId:
                    description: TenantId Your customer tenant id
                    type: string
                  vHostId:
                    description: VHostId ID of host system
                    format: int64
                    type: integer
                  vHostName:
                    description: VHostName Name of host system
                    type: string
                  vHostNumber:
                    description: VHostNumber Number of host system
                    format: int64
                    type: integer
                required:
                - addOns
                - additionalIps
                - cancelDate
                - cpuCores
                - createdDate
                - customerId
                - dataCenter
                - diskMb
                - displayName
                - imageId
                - instanceId
                - ipConfig
                - macAddress
                - name
                - osType
                - productId
                - productName
                - productType
                - ramMb
                - region
                - regionName
                - sshKeys
                - status
                - tenantId
                - vHostId
                - vHostName
                - vHostNumber
                type: object
              privateNetworks:
                description: PrivateNetworks to create and/or attach to the instance.
                items:
                  description: PrivateNetworkResponse defines model for PrivateNetworkResponse.
                  properties:
                    availableIps:
                      description: AvailableIps The total available IPs of the Private
                        Network
                      format: int64
                      type: integer
                    cidr:
                      description: Cidr The cidr range of the Private Network
                      type: string
                    createdDate:
                      description: CreatedDate The creation date of the Private Network
                      format: int64
                      type: integer
                    customerId:
                      description: CustomerId Your customer number
                      type: string
                    dataCenter:
                      description: DataCenter The data center where your Private Network
                        is located
                      type: string
                    description:
                      description: Description The description of the Private Network
                      type: string
                    instances:
                      items:
                        description: Instances defines model for Instances.
                        properties:
                          displayName:
                            description: DisplayName Instance display name
                            type: string
                          errorMessage:
                            description: ErrorMessage Message in case of an error.
                            type: string
                          instanceId:
                            description: InstanceId Instance id
                            format: int64
                            type: integer
                          ipConfig:
                            description: IpConfig defines model for IpConfig.
                            properties:
                              v4:
                                description: IpV4 defines model for IpV4.
                                properties:
                                  gateway:
                                    description: Gateway Gateway
                                    type: string
                                  ip:
                                    description: Ip IP Address
                                    type: string
                                  netmaskCidr:
                                    description: NetmaskCidr Netmask CIDR
                                    format: int32
                                    type: integer
                                required:
                                - gateway
                                - ip
                                - netmaskCidr
                                type: object
                              v6:
                                description: IpV6 defines model for IpV6.
                                properties:
                                  gateway:
                                    description: Gateway Gateway
                                    type: string
                                  ip:
                                    description: Ip IP Address
                                    type: string
                                  netmaskCidr:
                                    description: NetmaskCidr Netmask CIDR
                                    format: int32
                                    type: integer
                                required:
                                - gateway
                                - ip
                                - netmaskCidr
                                type: object
                            required:
                            - v4
                            - v6
                            type: object
                          name:
                            description: Name Instance name
                            type: string
                          privateIpConfig:
                            description: PrivateIpConfig defines model for PrivateIpConfig.
                            properties:
                              v4:
                                items:
                                  description: IpV4 defines model for IpV4.
                                  properties:
                                    gateway:
                                      description: Gateway Gateway
                                      type: string
                                    ip:
                                      description: Ip IP Address
                                      type: string
                                    netmaskCidr:
                                      description: NetmaskCidr Netmask CIDR
                                      format: int32
                                      type: integer
                                  required:
                                  - gateway
                                  - ip
                                  - netmaskCidr
                                  type: object
                                type: array
                            required:
                            - v4
                            type: object
                          productId:
                            description: ProductId Product id
                            type: string
                          status:
                            description: Status State of the instance in the Private
                              Network
                            type: string
                        required:
                        - displayName
                        - instanceId
                        - ipConfig
                        - name
                        - privateIpConfig
                        - productId
                        - status
                        type: object
                      type: array
                    name:
                      description: Name The name of the Private Network
                      type: string
                    privateNetworkId:
                      description: PrivateNetworkId Private Network's id
                      format: int64
                      type: integer
                    region:
                      description: Region The slug of the region where your Private
                        Network is located
                      type: string
                    regionName:
                      description: RegionName The region where your Private Network
                        is located
                      type: string
                    tenantId:
                      description: TenantId Your customer tenant id
                      type: string
                  required:
                  - availableIps
                  - cidr
                  - createdDate
                  - customerId
                  - dataCenter
                  - description
                  - instances
                  - name
                  - privateNetworkId
                  - region
                  - regionName
                  - tenantId
                  type: object
                type: array
              ready:
                description: Ready is true when the provider resource is ready.
                type: boolean
              secrets:
                description: Secrets to create and/or attach to the instance.
                items:
                  description: SecretResponse defines model for SecretResponse.
                  properties:
                    createdAt:
                      description: CreatedAt The creation date for the secret
                      format: int64
                      type: integer
                    customerId:
                      description: CustomerId Your Customer number
                      type: string
                    name:
                      description: Name The name assigned to the password/ssh
                      type: string
                    secretId:
                      description: SecretId Secret's id
                      type: number
                    tenantId:
                      description: TenantId Your customer tenant id
                      type: string
                    type:
                      description: Type The type of the secret. This will be available
                        only when retrieving secrets
                      type: string
                    updatedAt:
                      description: UpdatedAt The last update date for the secret
                      format: int64
                      type: integer
                    value:
                      description: Value The value of the secret. This will be available
                        only when retrieving a single secret
                      type: string
                  required:
                  - createdAt
                  - customerId
                  - name
                  - secretId
                  - tenantId
                  - type
                  - updatedAt
                  - value
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
